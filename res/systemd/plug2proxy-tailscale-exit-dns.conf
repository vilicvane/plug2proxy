[Unit]
# Exit-node DNS is delivered to the host resolver by tailscaled. Keep the
# resolver alive and restart Plug2Proxy after it restarts so `network apply`
# can recreate the owned runtime-only DNS route.
Wants=systemd-resolved.service
After=systemd-resolved.service
PartOf=systemd-resolved.service
