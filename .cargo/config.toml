[build]
target = "x86_64-unknown-linux-gnu"

[target.x86_64-unknown-linux-gnu]
linker = "x86_64-linux-gnu-gcc"

# Use cross for building release binaries with better glibc compatibility
[alias]
release = "build --release"
