{
  "name": "plug2proxy",
  "private": true,
  "license": "MIT",
  "author": "vilicvane",
  "scripts": {
    "build": "rimraf packages/plug2proxy/bld && tsc --build",
    "lint": "eslint .",
    "lint-prettier": "prettier --check .",
    "test": "yarn lint-prettier && yarn build && yarn lint"
  },
  "workspaces": [
    "packages/plug2proxy"
  ],
  "devDependencies": {
    "@mufan/code": "^0.2.10",
    "@mufan/eslint-plugin": "^0.1.48",
    "eslint": "^8.3.0",
    "prettier": "^2.5.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.2"
  }
}
